# Web framework
Flask==2.3.6          # یا اگر FastAPI هست: FastAPI==0.111.1

# For request handling (HTTP)
requests==2.32.0

# For templates and HTML rendering
Jinja2==3.1.3

# Database (SQLite is built-in, فقط اگر SQLAlchemy استفاده شده)
SQLAlchemy==2.0.22

# For environment variable handling
python-dotenv==1.0.0

# CORS support if using APIs
Flask-Cors==3.1.1     # برای Flask
# اگر FastAPI: 
# fastapi[all]==0.111.1

# Optional: for password hashing or authentication
passlib==1.7.4

# Optional: if you use async support in FastAPI
# uvicorn==0.24.0

# Optional: if using HTTP forms or file uploads
Werkzeug==3.2.3

# Optional: if you plan logging
loguru==0.7.0

